import { Routes } from '@angular/router';
import { ClinicOutcomesComponent } from './features/clinic-outcomes/clinic-outcomes.component';

export const routes: Routes = [
  { path: '', component: ClinicOutcomesComponent }
];